你正在观察一张完整的现实一番赏版面。只返回一个 JSON object。不要输出推理、解释、Markdown、坐标或中间证据。

<scene_model>
这是一张现实中的一番赏版面。提取票数前，先理解票栏的物理结构。

需要区分：
1. 原始印刷底板；
2. 后来贴上的实体赏票；
3. 尚未贴票、仍露出的空票位置；
4. 表示编号、剩余数量或状态的印刷文字。

只有后来贴上的实体赏票才算已贴票。底板本身印刷的赏级字母、赏级名称、奖品文字或占位内容，都不能单独算作实体票。

在赏票贴付/回收区域内，黑色或深色的票状长条是已贴上的实体赏票，不是空票位。

实体票可以相互叠贴。后一张可能盖住前一张的大部分，使前一张只露出赏级票头、字母甚至部分字母；只要能从叠贴结构确认它属于一张独立的底层实体票，就计 1 张。一个叠贴段最后完整露出的实体票本身也计 1 张。

票栏可以横向、纵向、换行、换列，或分成多个连续区域；方向只影响布局，不改变票的含义。计数前必须查看该赏级的完整票栏。

未贴票位可能表现为“第N张”、あとN枚、残りN枚、印刷占位或其他未被实体票覆盖的底板区域。

即使多个赏级区域显示完全相同的 rawLabel，只要它们在版面上是彼此独立的物理赏级区域，就必须作为多个独立 tier item 返回，不能合并。

只使用当前图片中能确认的证据。不要根据一番赏常见价格、常见总抽数或常见赏级数量猜测；无法可靠识别时返回 null。
</scene_model>

只提取以下视觉事实：
{
  "ipName": "string|null",
  "ipRawText": "string|null",
  "themeName": "string|null",
  "price": "number|null",
  "tiers": [
    {
      "rawLabel": "string|null",
      "prizeName": "string|null",
      "totalTickets": "integer|null",
      "pastedTickets": "integer|null"
    }
  ]
}

字段规则：
- ipName 是作品/IP 根名称；ipRawText 保留可见的原始身份文字；themeName 是本次篇章、系列、周年、Vol 或副标题，不能只是重复 ipName。
- price 只返回图片中可见的单抽价格数值；未显示或无法可靠判断时返回 null。
- tiers 按视觉阅读顺序列出每一个视觉上独立的赏级区域。rawLabel 保留图片上足以区分 A、A1、A2、D1、SP賞 等的原始文字。不要合并 A1/A2，不要计算 parent tier，不要自行命名 SP1/SP2。
- 即使 rawLabel 相同，只要是不同的实体赏级区域，就必须保留为不同数组 item，包括多个 SP賞。
- tiers 不包含 Last One/最终赏、Double Chance、说明文字、标题或状态标签。
- totalTickets 是该视觉赏级区域的总票容量；pastedTickets 是该区域后来贴上的实体票数量。两者必须独立观察。
- null 表示未知；0 表示视觉上确认是零。禁止把未知变成 0、把 totalTickets 复制给 pastedTickets、计算 remaining 或输出整板合计。
- 全X種、全X款及其他款式数量描述奖品 variant，不是票容量。
- 只使用完整当前图片。不要根据常见价格、常见 60/70/80 总抽数、奖品稀有度或常见赏级数量猜测。

JSON object 必须恰好包含上面的五个根字段；每个 tier item 必须恰好包含上面的四个字段。只返回 JSON。
