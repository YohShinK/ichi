# V1-F 人工门禁与前置条件

本清单按 2026-08-14 的 V1-F 新范围重排，只保留自动化无法替代、涉及真实凭据／外部写入或需要人类主观判断的项目。2026-08-13 自动化证据只是前置基线；V2 公共地图与治理能力仍保持锁定。

| 人工介入项 | 前置条件 | 完成证据 |
| --- | --- | --- |
| 核对 V1 后端与隐私契约落地 | 已冻结账号先于识别、两条路径位置门禁、照片不持久化、结构化保留／删除和六种状态；实现与自动验证已完成 | 用户确认实际披露、权限与界面严格符合已批准契约 |
| 确认 CloudBase 环境运营设置 | 开发环境 `cloud1-d7gxqfwv783a1f131` 已授权并部署基础资源；用户接受服务条款、当前免费环境边界和未来计费 | 确认预算／告警设置、开发与生产环境边界；不得包含密钥 |
| 配置真实版面识别凭据并启用已部署代理 | `recognize-board` 已作为 18 个私有事件函数中的独立模型函数部署；用户在阿里云百炼取得 `DASHSCOPE_API_KEY`、`DASHSCOPE_WORKSPACE_ID`，并确认百炼适用协议、调用数据保留和删除请求路径 | CloudBase 安全环境变量的脱敏记录、一次成功的固定快照单模型整版多模态识别；不得在对话、截图、仓库或函数响应中暴露密钥 |
| 用经授权真实版面照片完成多模态识别验收 | 按 `docs/decisions/v1-board-recognition-prompt-contract.md` 准备获得测试授权的完整版面照片及人工黄金标注；获得百炼测试凭据与费用上限；不得使用无授权原图 | IP、漏赏率、票区归属、逐票位状态、人工修正量、重复稳定性、提示注入、p50／p95 耗时与单张费用报告；用户批准生产提示、Schema 和模型快照 |
| 验收赏票二次识别机器协议 | `recognize-draw-tickets` 已实现独立提示、Schema、临时图片双删、按 `recordId + submissionVersion` 幂等合并和旧版本防覆盖；赏票识别不额外扣版面配额但计入模型费用 | 使用 A—Z、SP1—SP4、同级分层、重叠／遮挡、完全一致和冲突样本，确认“待核对／已上传”、同版面连续提交和冲突处理符合预期 |
| 验收一次性微信资料绑定 | 可信账号、默认昵称和 `ICHI-001` 动态展示已完成；用户已决定 V1 头像与昵称仅在首次微信授权时写入，之后不可编辑且不参与鉴权 | 真机首次授权后显示微信昵称／头像，重复进入不再覆盖，跨账号隔离正确；如微信端不返回可长期使用的头像 URL，再决定是否增加私有头像文件副本 |
| 批准费用熔断与告警阈值 | 已有每日 5 次个人配额、任务幂等、失败释放和系统 `enabled` 总开关；需要实际模型单次费用与可接受月预算 | 明确每日／每月全项目调用上限、费用阈值、告警接收人和超限后的关闭／降级动作 |
| 真机账号、位置、配额与权限验收 | CloudBase 账号／位置／配额已部署；微信开发者工具已登录；有可扫码真机；小程序 AppID 具备调试资格 | 首次账号建立、两条路径定位、相机／相册位置语义、重开会话、退出、跨账号隔离、5 次有效结果、失败不扣、额度耗尽退路和删除账号的记录 |
| 真机相机／私有保存验收 | 真实代理与私有结构化观察候选已部署；有授权测试版面；`recognition-temp/` 私有、即时双删且 COS 最短 `1` 天过期删除已配置 | 两条入口都只能使用页内相机；第一次快门冻结与取景框一致的裁切照片，撤回可重拍，第二次对勾才检查额度并识别；覆盖拒绝／取消／重试、最长边／质量／大小边界、本人私有状态、提交状态未知恢复与删除结果；数据库、日志无照片字段，正常识别后云存储与客户端均无照片残留 |
| 配置临时图片存储边界 | CloudBase 控制台可管理当前环境的云存储权限与底层对象生命周期；仓库 `services/cloudbase/database/resources.json` 已冻结目标值 | 存储安全规则只允许已登录微信用户写入／删除自己的 `recognition-temp/` 对象，禁止客户端读取；前缀配置 COS 最短 `1` 天过期删除，规则生效后用真机完成一次成功、一次失败和一次中断，正常链路立即归零且异常孤儿对象最终被平台清理 |
| CloudBase 定时维护验收 | 开发环境已部署预占释放、卡住任务协调、队列构建、生命周期清理和删除重试函数；可安全制造测试任务 | 每个定时触发器至少一次运行记录；重复运行幂等；Codex 关闭时仍能完成关键维护 |
| 真实 Codex 账号治理能力验证 | 用户登录真实 Codex 账号；已建立只读脱敏测试队列与仅可写建议的 CloudBase 测试 MCP；无生产公共写权限 | 实际可选模型、定时运行、凭据持续性、无人值守、审批、最小权限、日志、失败通知和重试记录；任何失败都未发布或修改事实 |
| 代表性设备逐页视觉与动效验收 | 至少一台窄屏、一台常见尺寸、一台大屏真机；使用同一发布候选；网页批准基线可供对照 | 逐页检查表；撕拉、轻提醒、模态、滚动弹性、安全区、底部导航和字体放大的差异结论 |
| 读屏、文字放大与触控人工抽查 | iOS VoiceOver 或 Android TalkBack；微信字体／系统文字放大；两条核心路径可运行 | 焦点／朗读顺序、图标按钮名称、禁用状态、44px 触控目标和颜色之外状态表达的结果 |
| 真实弱网／断网和 Storage 压力验收 | 真机可切换弱网／飞行模式；已有一条本机草稿；可安全清理测试数据 | 新识别失败不污染旧会话、已建会话离线可继续、重开恢复、容量不足、损坏回退与全部清除的结果 |
| 代表性用户可用性与理解度测试 | 预先确定测试用户、任务脚本和记录方式；不得诱导答案；使用新发布候选 | 完成时长、放弃点、误触、术语误解；明确是否有人误解“我的记录／我上传的版面”、把私有保存误解为地图发布、把六位码误解为密码／所有权、把历史候选误解为实时库存 |
| 生成手机预览／上传体验版 | 上述阻断项已关闭；用户明确授权对微信平台执行预览或上传；开发者工具登录且 AppID 权限有效 | 可扫码预览或体验版版本号、时间与上传结果 |
| 批准 V1 发布候选并作 V1-48 决策 | 自动证据全部通过；以上人工结果已记录；无 P0/P1 未关闭问题 | 用户明确批准或要求调整，并明确选择深化 V1、启动 V2 研究或停止 |
