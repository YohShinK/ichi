# V1-F 发布候选清单

> 2026-08-30 closure：`COMPLETED / ARCHIVED`。V1.0.0 发布候选、真机验收、release commit、备案／上架准备、微信 upload／送审、审核通过与正式发布均已完成；本文件保留构建候选时的历史门禁，不再承担 current status。V1 active plans=`0`。

> 2026-08-14 已按 V1 账号、每日配额与私有观察新范围重置。2026-08-13 的勾选证据只能复用，以下项目必须在新实现后重新通过。

## 自动门

- [x] 双入口核心行为、账号先行、位置门禁、每日配额、识别可空编辑／严格提交、本机／云端状态自动回归
- [x] 网页 Playwright 26 项基线、Next.js 生产构建与小程序 WXML／WXSS 静态／开发者工具编译入口
- [x] 版面 schema、识别契约、A—Z／OTHER 降级和组件注册表
- [x] 千问 `qwen3.7-flash` 单模型、固定 Prompt／Schema／Normalize、真实识别与失败闭合已完成并冻结；后续精度／性能优化转入 Post-V1
- [x] 照片只短暂进入私有 `recognition-temp/`，即时清理与 Storage CUSTOM／COS `Days=1` 兜底均已完成并读回
- [x] 账号隔离、配额原子预占／扣除／释放、幂等和生产费用边界已随 V1 发布冻结关闭
- [x] 服务端六位观察记录码、旧码迁移、`recordId`／`boardId` 幂等与两类记录隔离
- [x] 版面与赏票照片只存在于当次识别链路及私有临时对象，不写数据库、日志、备份或长期对象；即时双删、平台 1 天兜底及删除重试边界已关闭
- [x] V2 历史候选资格预检可重放；较早记录不因时间被拒绝，缺授权／位置、冲突或隐私不合格记录不被误标可公开
- [x] CloudBase 定时触发器覆盖预占释放、卡住任务协调、私有治理报告与删除重试，且不依赖 Codex 运行
- [x] 500 条本机记录、连续 50 抽／撤销、离线、损坏、容量和重启恢复
- [x] 语义标签、禁用状态、读屏提示和关键触控目标静态检查
- [x] 小程序源码小于 2 MiB，sitemap 合法

## 人工门

- [x] 百炼真实密钥与工作空间 ID 已由用户配置在 CloudBase 服务端环境；新版识别代理和 18 个函数均已完成线上 `Deployment completed`，真实照片准确度／耗时／费用仍在后续人工门验收
- [x] CloudBase Storage CUSTOM 与 `recognition-temp/` COS `Days=1` 已配置、读回并关闭
- [x] 账号先行、位置授权、照片不持久化披露、结构化保留／删除和状态机已随真机冻结关闭
- [x] 经授权真实照片核验与 same-version retry 已完成关键真机验收
- [x] 真机相机／权限、弱网／恢复和 Storage 关键 blocker 已关闭
- [x] 页面视觉、交互和最终 UI 已由用户批准进入 V1.0.0
- [x] 可访问性与触控自动门已通过；无剩余 V1 blocker
- [x] 独立代表性用户研究门由最终用户验收与发布决定取代，不再是 V1 blocker
- [x] Codex/Luna 治理验证已转入 V2 backlog，不属于 V1 发布范围
- [x] 微信 upload prerequisite 已完成，V1.0.0 已送审
- [x] 用户批准 V1.0.0 发布候选并完成 V1 freeze 决策

任何 V2 现实版面自动合并、Luna 治理、来源审核、公共地图写入或地图发布均不属于本候选。

## 公共信息防污染边界（V2 输入，不在 V1 执行）

- 来源身份：由未来认证会话绑定 `ownerAccountId`；六位辅助码和客户端字段不承担鉴权。
- 证据：保留合法来源、版面照片授权、字段守恒和必要的赏票核对证据。
- 审核：仅本机、待同步、私有未核对或来源不明的记录始终留在私人域。
- 重复检测：未来同时检查重复图片、重复规范记录和同一现实版面冲突；`boardId` 与短码不能单独去重。
- 版本与时间：公共状态必须携带观察时间、版本和冲突处理；较早记录仍可作为历史观察，但不能被称为实时库存。
- 所有权：`boardId` 只负责同一次本机版面链路幂等；未来 `recordId` 标识规范记录；六位码只供查找，三者都不证明现实版面排他所有权。
