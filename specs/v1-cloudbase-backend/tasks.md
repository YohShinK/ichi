# V1 CloudBase 后端任务拆分

> 状态：已批准实施
>
> 执行方式：先在仓库中整体完成并验证，再作为一个受控部署批次写入 CloudBase；以下细分项用于开发、测试和追踪，不要求逐项在控制台操作。

## T0 正式事实源对齐

- [x] 将“不持久化照片、只保存结构化快照与抽赏差量”同步到 design document。
- [x] 将 CloudBase、无照片链路、千问服务端调用和安全边界同步到 tech stack。
- [x] 重写 V1-F 实施步骤、状态和架构文件中的旧压缩图片方案。
- [x] 标记 V2 公共地图、公共快照和发布接口仍为锁定。

验证：工作流校验通过；正式文档不存在把压缩图片作为 V1 已确认方案的冲突描述。

## T1 数据契约与机器可读 Schema

- [x] 定义账号、资料、ICHI ID、角色、配额、识别任务、观察候选、抽赏提交、六位码、删除和审计类型。
- [x] 定义 `BoardSnapshot`、`DrawSubmission` 和模型返回 JSON Schema。
- [x] 写入 A1/A2 等基础奖级归并与 SP1—SP4 独立规则。
- [x] 定义状态机、错误码、问题码、版本字段与兼容策略。
- [ ] 用真实多版面、同级分层、SP 赏和不规则贴票样本建立黄金用例。

验证：Schema 正反例、归一化用例、兼容性测试全部通过。

## T2 CloudBase 数据资源清单

- [x] 在仓库定义全部集合、确定性主键和索引。
- [x] 定义客户端不可伪造所有权的私有权限规则。
- [x] 定义 `ICHI-001—999` 保留登记和非秘密系统设置种子。
- [x] 明确不创建 `recognitionEvidenceAssets` 和业务图片存储目录。
- [x] 编写资源 dry-run、差异检查和幂等部署脚本。

验证：资源清单静态验证通过；重复 dry-run 得到稳定结果。

## T3 共享后端基础库

- [x] 实现可信微信身份解析与内部账号解析。
- [x] 实现事务、幂等、审计、时间、错误响应和日志脱敏。
- [x] 实现每日配额状态机和全局熔断。
- [x] 实现六位记录码和普通 ICHI ID 的无碰撞分配。
- [x] 实现奖级归一化、守恒检查和最终快照推导。

验证：并发、碰撞、重试、越权、守恒和日志脱敏单元测试通过。

## T4 账号、资料与特殊 ICHI ID

- [x] 实现 `bootstrap-account`。
- [x] 实现 `get-my-profile`。
- [x] 实现 `assign-special-ichi-id` 和审计。
- [x] 实现创始人一次性领取 `ICHI-001` 的安全引导机制。
- [x] 实现 `bind-wechat-profile` 与前端微信头像／昵称授权；首次完成资料后不重复引导，用户从“我的”头像或用户名再次授权时可更新展示资料，资料不参与鉴权且不能改变所有权。

验证：并发首次建号、重复登录、特殊号分配、旧号别名、无权限拒绝和页面展示测试通过。

## T5 配额与初始版面识别

- [x] 实现 `get-quota-status`、`reserve-recognition` 和 `get-recognition-job` 的账号／配额／任务基础。
- [x] 将 `recognize-board` 从旧双模型代理迁移到批准后的 rc1 单模型协议并部署到开发环境。
- [x] 在仓库代理中接入 `qwen3.7-flash` 单模型非思考 JSON Object 请求；使用 `ichi-board-vlm-3.0.0-rc1` 与 `board-provider-extraction-3.0.0-rc1`，再由服务端 AJV 与 Normalize 保证业务契约。
- [x] 图片以二进制短暂进入私有 `recognition-temp/`，同步事件只传任务绑定 `fileID`；禁止数据库／日志／长期对象落盘，旧局部 OCR 裁片代码已移除。
- [x] 实现多版面边界选择、中心/面积优先和无法唯一确定时的重拍状态。
- [x] 实现两条入口共享每天 5 次配额的原子预占、一次性任务令牌、单次抢占、成功扣次、失败即时释放和卡住任务租约回收；全局费用阈值仍待人工批准后配置。

验证：成功、失败、超时、取消、重试、重复幂等键、跨日和熔断测试通过；环境中不产生图片对象。

## T6 初始观察确认与保存

- [x] 实现 `finalize-board-observation`。
- [x] 服务端校验 IP、价格、必填票数、已贴票数允许为 0、奖级和守恒。
- [x] 原子生成 `recordId`、六位码、初始快照、位置/时间/披露版本和审计事件。
- [x] 直接上传路径将初始快照作为当前最终快照。
- [x] 辅助抽赏路径保留同一 `boardId/recordId` 等待后续更新。

验证：缺失字段、非法范围、重复提交、六位码冲突、位置缺失和越权测试通过。

## T7 抽赏后识别与差量推导

- [x] 实现 `recognize-draw-tickets`，复用同一模型但采用独立 `ichi-draw-ticket-vlm-1.0.0-rc1` 任务契约。
- [x] 保存小程序累计抽取计数、模型结构化结果、版本差异和对账状态；照片仍为临时对象。
- [x] 实现 A1/A2 等归并、SP1—SP4 独立及逐级减法。
- [x] 小程序历史与赏票事实不一致时进入 `needs_user_confirmation`，不静默覆盖最终快照。
- [x] 实现 `finalize-draw-update`，幂等保存最终快照和推导版本。
- [x] 赏票识别不额外扣除版面识别配额；提交版本严格递增，同版本重试幂等，旧异步结果标记 `superseded`，模型成本写入独立调用日志／后续费用汇总。

验证：一致、冲突、超额、负数、未知赏级、守恒失败、撤回历史和重复提交测试通过。

## T8 本人记录、删除与后台维护

- [x] 实现 `get-my-records`、`delete-my-record` 和 `delete-my-account`。
- [x] 实现 `release-stuck-reservations`、`reconcile-stuck-jobs` 和 `retry-deletions`。
- [x] 实现只生成私有资格报告的 `prepare-v2-backfill`。
- [x] 识别任务、账号、配额、冲突、删除和审计均保留可查询状态／时间／错误字段；费用阈值、告警接收人和超限动作待人工批准后配置。
- [ ] 确保客户端和识别函数在正常／失败链路双重删除临时图片，并在控制台为异常退出配置 `recognition-temp/` 的 COS 最短 `1` 天过期删除兜底；代码与自动验证已完成，云端规则仍待人工配置。

验证：跨账号隔离、分页、删除时限、部分失败重试、卡住任务修复和无公共写入测试通过。

## T9 CloudBase 开发环境后端部署与独立验证

- [x] 再次只读核对目标开发环境和 AppID。
- [x] 按仓库清单创建集合、索引、私有权限和非秘密种子。
- [x] 部署全部后端事件函数与维护触发器，不等待前端改造。
- [ ] 独立验证账号、资料、配额、任务幂等、观察保存、本人查询／删除和维护接口；正式微信账号、`ICHI-001`、配额摘要、四个维护入口、一次性识别任务令牌、伪造任务拒绝及无凭据即时释放已通过，成功观察、跨账号与删除链仍待真实调用。
- [x] 核对数据库、日志、函数和存储中没有照片、公开集合或公共发布入口。

验证：开发环境资源与仓库清单一致；后端接口在没有新前端的情况下可独立调用并通过权限、幂等与失败闭合检查。

## T10 小程序端到端接入

- [x] 所有识别入口先完成静默账号建立和位置授权。
- [x] 首次资料未完成时显示“使用微信登录”；授权后动态展示微信头像／昵称和规范 ICHI ID；点击头像或用户名可再次授权更新，资料不参与鉴权且不改变 ICHI ID 或记录归属。
- [x] 接入 `get-quota-status` 的每日 5 次左上角分数环和单操作 `QUOTA_EXHAUSTED` 弹窗。
- [x] 将测试识别适配器切换为真实私有云函数适配器，并仅在 `develop` 环境保留不写云端的固定票池回退。
- [x] 接入版面配额预占、任务令牌、异步任务恢复、初始观察确认、本人记录同步和抽赏后独立赏票机器协议。
- [x] 为无网络、拒绝位置／相机权限、配额用尽、模型失败、私有保存失败和删除中提供既有设计规范内的状态；赏票冲突状态随 T7 完成。
- [x] 确认本地草稿与云端记录的边界和命名：只使用“待上传／待核对／已上传”，上传页命名为“我上传的版面”；本人云端记录经 owner-scoped 云函数读取／左滑删除并按 `boardId` 去重。

验证：小程序单元/行为测试、WXML/WXSS 编译、类型、Lint、真机前自动回归通过。

## T11 文档、安全审查与总回归

- [x] 编写 `docs/delivery/v1-cloudbase-backend-guide.md`，面向人解释云端资源、账号、配额、识别、数据、删除、运维和费用。
- [x] 生成精确部署清单和回滚/停止条件。
- [x] 执行 CloudBase 专项代码审查、密钥扫描、权限审查和日志审查。
- [x] 执行仓库全量测试、类型、Lint、构建、契约、工作流和差异检查。
- [x] 将验证结果写入 progress。

验证：全部自动门禁通过，用户批准部署清单。

## T12 CloudBase 部署复核与配置收口

- [x] 复核开发环境资源、权限、函数、触发器与仓库清单的差异。
- [x] 对当前后端／前端联调产生的 `reserve-recognition`、`recognize-board`、`get-recognition-job` 和 `finalize-board-observation` 代码变化执行受控更新。
- [x] 用户已在 CloudBase 安全配置中录入千问 API 密钥与工作空间 ID；仓库与日志不保存明文。
- [x] 运行部署后结构、权限、函数和定时触发器检查。
- [x] 本轮没有发现权限或资源差异；如后续出现差异则按停止条件中断，不继续扩散变更。

验证：资源与仓库清单一致；数据库、函数和存储仍无公开写入面；业务存储中无图片对象。

## T13 真实环境验收

- [x] 创始人真机首次登录并领取 `ICHI-001`。
- [ ] 真机验证相机、位置、两条入口和每天 5 次配额。
- [ ] 使用黄金版面核验模型输出、用户修订和 SP/同级分层规则。
- [ ] 完成一次辅助抽赏并核验最终快照等于初始快照减确认抽中票。
- [ ] 验证本人查询/删除、账号隔离、失败恢复和全局熔断。
- [ ] 核对数据库、日志和云存储中没有照片残留。

验收：用户确认 V1-F 后端真实链路完成；V2 仍保持锁定，等待独立解锁。

## 人工介入清单与前置条件

| 人工事项                       | 前置条件                                    |
| ------------------------------ | ------------------------------------------- |
| 审阅本需求、技术设计和任务拆分 | 本轮文档完成                                |
| 配置千问 API 密钥与工作空间 ID | 函数代码和环境变量名已通过本地审查并部署    |
| 创始人领取 `ICHI-001`          | 已完成                                      |
| 批准相机与位置权限             | 真机安装正式开发版本                        |
| 提供/确认黄金样本答案          | 模型 Schema、提示词和归一化器已冻结候选版本 |
| 批准 CloudBase 整体写入        | 已完成，当前开发环境资源复核一致            |
| V1-F 最终人工验收              | 真实环境端到端和删除/隔离检查完成           |
